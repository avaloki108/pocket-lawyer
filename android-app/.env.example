# Example environment variables for Pocket Lawyer Flutter app
# Copy this file to .env and fill in your real keys. NEVER commit the real .env.

OPENAI_API_KEY=your-openai-api-key
LEGISCAN_API_KEY=your-legiscan-api-key
CONGRESS_GOV_API_KEY=your-congress-gov-api-key
# Optional / future integrations
PINECONE_API_KEY=your-pinecone-api-key
COURTLISTENER_API_KEY=your-courtlistener-api-key

# OpenRouter fallback (used only if backend OpenAI proxy call fails)
OPENROUTER_API_KEY=your-openrouter-api-key
# Optional: override model (default set internally if blank)
OPENROUTER_MODEL=openai/gpt-4o-mini
